# {{PROJECT_NAME}}

> AI Agent 入口。其他 AI 工具（CLAUDE.md / GEMINI.md / CODEBUDDY.md）通过软链接读取本文件。
> **目标 ≤200 行**。超出视为设计失败，必须拆分到 `harness/*.md`。
> 详细规约下沉，本文件只做硬约束 + 索引。

## Identity

- **项目定位**：{{一句话定位 — 做什么 / 给谁用 / 核心价值}}
- **仓库类型**：{{single-service / monorepo / library / CLI}}
- **AI 角色**：高级开发者，遵守本文件所有约束；产出经 hook 验证。

## Stack

| 层 | 技术 | 版本 |
|----|------|------|
| 语言 | {{Go / TS / Python}} | {{≥1.22}} |
| 框架 | {{Gin / Next / FastAPI}} | {{x.y}} |
| 数据库 | {{PG / MySQL}} | {{x.y}} |
| 构建 | {{make / pnpm / cargo}} | - |
| CI | {{GitHub Actions / Jenkins}} | - |

## Commands

```bash
{{make deps}}     # 安装依赖
{{make dev}}      # 本地启动
{{make test}}     # 全部测试
{{make e2e}}      # 端到端测试（完成定义的金标准）
{{make lint}}     # 代码检查
{{make build}}    # 构建
make check        # 完整门禁（test + lint + build）
```

## Structure

```
{{项目根}}/
├── cmd/         # 入口
├── internal/    # 私有逻辑
├── pkg/         # 可复用
├── api/         # 协议定义
├── harness/     # 开发规约（AI + 人共读）
├── .pge/        # PGE 档 2/3 工件（按需）
└── .harness/    # 状态文件（机器可读）
```

## Rules（违反即 BLOCK，hook 强制）

1. AGENTS.md 行数 ≤200（razor BLOCK；超出拆到 `harness/*`）
2. PROGRESS.md 同时只能有 1 个 `- [~]` in-progress 标记（WIP=1）
3. 测试断言不可被 AI 修改（test guard hook 检测）
4. main 分支不直接改动（feature branch + PR/merge）
5. 提交信息 `type(scope): message`，关联 Issue
6. sub-agent 嵌套深度 ≤2，并行宽度 ≤5
7. E2E 通过才算完成（仅过单测不算）

## Workflow（10 步，AI 严格遵守）

1. **读 AGENTS.md + 相关 `harness/*.md`**——动手前的硬约束
2. **查 [failures.md](harness/failures.md)**——避免重复踩坑
3. **判 PGE 档位** → 见 [development.md](harness/development.md) §PGE
   - 档 1：≤2h 单文件，单会话
   - 档 2：多文件 / 跨会话，feature branch + sub-agent + `.pge/spec.md`
   - 档 3：跨天 / `/loop`，档 2 + `.harness/campaign.json`
4. **判 Spec 重量** → SpecKit / OpenSpec / 直接做（小型必带回归测试）
5. **TDD 优先**：功能代码前先写 `*_test.go` 失败测试（Red）
6. **最小变更实现**：让测试通过（Green），不做"顺便重构"
7. **过 `make check`**：本地全部通过（test + lint + build）
8. **过 `make e2e`**：完成定义；后端用契约测试 + 影子流量
9. **更新状态文件**：PROGRESS.md（what）+ DECISIONS.md（why）
10. **沉淀踩坑**：满足触发条件即写 `harness/failures.md`

### 踩坑触发（满足任一即必须记录）

- 排查 > 10 分钟的非显然 bug
- 生成代码被人工纠正
- 环境/依赖导致的隐蔽问题
- revert 之前的提交
- 同一区域代码反复修改

格式见 [failures.md](harness/failures.md) 头部。

### Sub-agent 调用约束（PGE 档 2/3）

- 深度 ≤2 层（主 → sub → sub-sub）
- 单 message 并行 ≤5
- 必须文件交接（写 `.pge/*.md`），禁止 SendMessage
- 短命 sub-agent 退出后状态写入文件
- E 启动时只给 `.pge/spec.md` + git diff，不给 G 实现笔记

## Prohibitions

| ❌ 禁止 | ✅ 替代 |
|--------|---------|
| `fmt.Println` 调试 | 结构化日志 |
| 跳过测试提 PR | `make check` 通过 |
| `git commit --no-verify` | 修复 hook 报错再提交 |
| 修改测试断言让自己通过 | 修代码不修测试 |
| AGENTS.md 加新规则 | 加到 `harness/*` 或升级到 hook |
| sub-agent 间 SendMessage | 文件交接 |
| 提交 `.env` / `*.key` / `vendor/` | `.env.example` |

## Patterns（决策表）

| 场景 | 选择 | 参考 |
|------|------|------|
| 新增 API | {{REST / gRPC}} | [api-standards.md](harness/api-standards.md) |
| 异步处理 | {{MQ / channel / 事件总线}} | [development.md](harness/development.md) |
| 数据校验 | validator tag | [coding-style.md](harness/coding-style.md) |
| 错误码 | {{集中 errcode 包}} | [api-standards.md](harness/api-standards.md) |
| DB 变更 | migration + review | [database.md](harness/database.md) |
| 文档画图 | Mermaid 优先；不支持的可 PlantUML / SVG | - |

## Index（详细规约）

| 规约 | 适用 | 文件 |
|------|------|------|
| 编码规范 | 命名 / 格式 / import | [coding-style.md](harness/coding-style.md) |
| 测试 | 覆盖率 / mock / 黄金样本 | [testing.md](harness/testing.md) |
| 术语表 | 业务名词消歧义 | [glossary.md](harness/glossary.md) |
| 踩坑记录 | 历史问题与解法 | [failures.md](harness/failures.md) |
| 开发流程 | PGE 档位 / Spec 重量 | [development.md](harness/development.md) |
| API 设计 | 接口 / 版本 / 错误码 | [api-standards.md](harness/api-standards.md) |
| 数据库 | DDL / 索引 / migration | [database.md](harness/database.md) |
| 部署 | 发布 / 回滚 | [deployment.md](harness/deployment.md) |
| 依赖关系 | 模块边界 | [dependency-map.md](harness/dependency-map.md) |
| Code Review | 评审标准 | [code-review.md](harness/code-review.md) |

## State Files（机器可读 + 冷启动友好）

| 文件 | 内容 | 改动者 |
|------|------|--------|
| [PROGRESS.md](PROGRESS.md) | 已完成 / 进行中（WIP=1）/ 阻塞 | AI 每会话尾更新 |
| [DECISIONS.md](DECISIONS.md) | 关键决策的"为什么" | AI + 人 |
| `.harness/metrics.tsv` | hook 写入的度量趋势 | hook |
| `.pge/spec.md` | sprint contract（PGE 档 2/3） | Planner |
| `.pge/eval.md` | 验证报告 | Evaluator |

## Docs

| 文档 | 读者 | 路径 |
|------|------|------|
| 项目介绍 | 人 | [README.md](README.md) |
| 架构设计 | 人 + AI | [ARCHITECTURE.md](ARCHITECTURE.md) |
| AI 规则 | AI（本文件） | [AGENTS.md](AGENTS.md) |
| 设计文档 | 人 + AI | `docs/` |

---

> 本文件由 [td-harness-init](https://github.com/...) skill 生成（v0.1）。
> 修改本文件等于修改硬约束——只增不减需团队评审。
