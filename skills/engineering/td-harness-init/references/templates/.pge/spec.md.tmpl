# Sprint Contract（{{TASK_NAME}}）

> PGE 档 2/3 的 P 阶段产出。**E 启动时仅读本文件 + git diff**——不读 G 的实现笔记。
> 文件交接是 sub-agent 的物理约束（见 sub-agent-physics）。

## Sprint 概要

- **目标**（Goal）：[一句话——做完什么算"做完"]
- **范围**（Scope）：[包含什么 / 不包含什么——负向声明很重要]
- **预估档位**：[档 1 / 档 2 / 档 3]
- **预估工期**：[≤2h / 半天 / 1-2 天 / 跨天]

## 验收标准（AC）

- [ ] AC-1：{{具体可验证的一条}}
- [ ] AC-2：{{...}}
- [ ] 单元测试覆盖率不降低
- [ ] `make check` 通过
- [ ] `make e2e` 通过（或契约测试）
- [ ] PROGRESS.md / DECISIONS.md 已更新
- [ ] failures 已沉淀（若有）

## Parallel Tasks（PGE 内层并行声明）

> **可选**——单任务删除本节。多任务必须声明，机器判定可并行。
> 充要条件：文件集不相交 + 依赖图无环 + 嵌套深度 ≤2。

```yaml
parallel_tasks:
  - id: 1
    desc: "{{任务 A 描述}}"
    files: [{{src/a.go}}, {{src/a_test.go}}]
    depends_on: []
  - id: 2
    desc: "{{任务 B 描述}}"
    files: [{{src/b.go}}, {{src/b_test.go}}]
    depends_on: []
  - id: 3
    desc: "{{用 A 和 B 拼装}}"
    files: [{{src/c.go}}]
    depends_on: [1, 2]
```

并行宽度上限 5；嵌套深度上限 2（sub-agent-physics）。

## 不变量（Invariants）

> 重构场景关键——必须列出"什么不能变"。

- {{对外 API 形态不变}}
- {{数据库 schema 不变}}
- {{黄金样本输入-输出 100% 通过}}

## 评估方式（给 E 的指引）

E 启动 prompt 应包含：
1. 本文件全文
2. `git diff main...feature/{{TASK_NAME}}`
3. **不**包含 G 的实现笔记或对话历史

E 输出到 `.pge/eval.md`，格式：
- PASS / FAIL
- 失败时：具体哪条 AC 未达 + 修复建议
- 维度评分（如启用 Scorecard）

## 关联

- [development.md](../harness/development.md) §PGE 档位
- [testing.md](../harness/testing.md) §完成定义
